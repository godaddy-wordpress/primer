sudo: false

language: php

notifications:
  email:
    on_success: never
    on_failure: change

branches:
  only:
    - /^v\d.*$/
    - master
    - develop

php:
  - 5.6

env:
  - WP_VERSION=latest WP_MULTISITE=0

matrix:
  fast_finish: true

install:
  - node -v
